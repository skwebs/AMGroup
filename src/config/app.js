import Config from 'react-native-config';
import API_BASE_URL from '../utils/config';

// encryption key
export const ENCRYPTION_KEY = Config.ENCRYPTION_KEY;

// base url for API requests
export const URL = API_BASE_URL;
